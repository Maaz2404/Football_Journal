name: Trigger Ingestion

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render trigger endpoint
        run: |
          curl -fsS "${{ secrets.RENDER_TRIGGER_URL }}"
